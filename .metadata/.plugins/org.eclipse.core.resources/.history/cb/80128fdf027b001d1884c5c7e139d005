package com.azaonlinefoodordering;

import java.util.Scanner;
import java.util.regex.Pattern;

public class SignUp {
	public String nameString = "^[\\w\\s\\w]{4,}";
	public String passString = "^\\w+(@|&)+\\d*";
	
	static Scanner sc = new Scanner(System.in);
	
	
	public static void main(String[] args) {
		SignUp pass = new SignUp();

//		String password, firstName, lastName;
//		do {
//
//			System.out.println("Enter a password :");
//			password = sc.next();
//		} while (!Pattern.matches(pass.passString, password));
//		
//		do {
//			System.out.println("Enter your name :");
//			firstName = sc.next();
//			lastName = sc.next();
//		} while (!Pattern.matches(pass.nameString, firstName+lastName));
//		
//		System.out.println(password +" "+ firstName +" "+lastName);
		
		System.out.println("Hello there welcome to the AZA online.\n Press 1 to log in."
				+"\n Press 2 to sign up."
				+"\n Press 3 to quit the application.");
		int in = sc.nextInt();
		switch (in) {
		case 1:
			System.out.println("Proceeding to login...");
			break;
		case 2:
			System.out.println("Proceeding to sign up...");
			break;
		case 3:
			System.exit(0);
		}
		
		boolean flag = true;
		while (flag) {
			String password, name;
			System.out.println("Please enter your full name: ");
			name = sc.next();
			System.out.println(name);
			if(Pattern.matches(pass.nameString, name)) {
				System.out.println("Lets go... "+ name);
				flag = false;
			}
		}
	}
}
